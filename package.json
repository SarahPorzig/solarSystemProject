{
  "name": "solarsystem",
  "version": "1.0.0",
  "description": "test site to practice using ajax calls from json data and npm packages",
  "main": "src/script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/script.js -o build/bundle.js"
  },
  "author": "sarah porzig",
  "license": "ISC"
}
